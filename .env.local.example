# Sanity project/dataset — not secrets (same values as studio/sanity.config.ts),
# only here so they can be overridden without a code change. lib/sanity/client.ts
# falls back to the values below if these are unset.
NEXT_PUBLIC_SANITY_PROJECT_ID=4ebwfvhx
NEXT_PUBLIC_SANITY_DATASET=production
